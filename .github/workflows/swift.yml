name: Build

on:
  push:
    branches: [ 'main', 'stable' ]
  pull_request:
    branches: [ 'main', 'stable' ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: macos-26
    strategy:
      matrix:
        destination: ['platform=iOS\ Simulator,OS=26.1,name=iPhone\ 17\ Pro\ Max', 'platform=watchOS\ Simulator,OS=26.1,name=Apple\ Watch\ Series\ 11\ \(46mm\)', 'platform=macOS CODE_SIGN_IDENTITY=""', 'platform=visionOS\ Simulator,OS=26.1,name=Apple\ Vision\ Pro CODE_SIGN_IDENTITY=""']
        scheme: ['CareKit', 'CareKitStore', 'CareKitUI', 'CareKitFHIR']
    steps:
      - uses: actions/checkout@v6
      - name: Set Xcode Version
        run: sudo xcode-select -s /Applications/Xcode_26.1.app
      - name: Build
        run: set -o pipefail && env NSUnbufferedIO=YES xcodebuild -verbose -workspace CKWorkspace.xcworkspace -scheme ${{ matrix.scheme }} -destination ${{ matrix.destination }} build test 2>&1 | xcbeautify --renderer github-actions
